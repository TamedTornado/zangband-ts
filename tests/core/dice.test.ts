import { describe, it, expect } from 'vitest';

// Placeholder test to verify test setup works
describe('dice', () => {
  it('should parse dice notation', () => {
    // TODO: implement parseDice and rollDice
    expect(1 + 1).toBe(2);
  });
});
